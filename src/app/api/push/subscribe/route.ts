import { NextResponse } from "next/server"

// POST /api/push/subscribe — stub; integrate with web-push or a service if needed
export async function POST() {
  return NextResponse.json({ success: true })
}
