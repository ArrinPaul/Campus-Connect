# =============================================================================
# Campus Connect - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Convex (Backend / Realtime Database)
# Get from: https://dashboard.convex.dev > Your Project > Settings
# Run `npx convex dev` once to auto-populate CONVEX_DEPLOYMENT and NEXT_PUBLIC_CONVEX_URL
# -----------------------------------------------------------------------------
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
# Used by GitHub Actions backup workflow
CONVEX_DEPLOY_KEY=

# -----------------------------------------------------------------------------
# Clerk (Authentication)
# Get from: https://dashboard.clerk.com > Your App > API Keys
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Get webhook secret from: Clerk Dashboard > Webhooks > Your endpoint > Signing Secret
CLERK_WEBHOOK_SECRET=

# Clerk routing (leave as-is unless you change the auth page paths)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/feed
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/feed

# -----------------------------------------------------------------------------
# Sentry (Error Monitoring)
# Get from: https://sentry.io > Your Project > Settings > Client Keys (DSN)
# SENTRY_AUTH_TOKEN: https://sentry.io > Settings > Auth Tokens (for source map uploads)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# -----------------------------------------------------------------------------
# PostHog (Product Analytics)
# Get from: https://posthog.com > Project > Settings > Project API Key
# -----------------------------------------------------------------------------
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# -----------------------------------------------------------------------------
# Upstash Redis (Rate Limiting)
# Get from: https://console.upstash.com > Create Database > REST API
# Used as Redis-backed rate limiter (falls back to in-memory if not set)
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Web Push Notifications (VAPID Keys)
# Generate via: npx web-push generate-vapid-keys
# NEXT_PUBLIC_VAPID_PUBLIC_KEY is safe to expose to the client
# VAPID_PRIVATE_KEY must remain server-side only - never expose to client
# -----------------------------------------------------------------------------
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# -----------------------------------------------------------------------------
# Build / CI Flags (optional)
# -----------------------------------------------------------------------------
# Set to "true" to enable bundle analyzer during `npm run build`
ANALYZE=false
# Automatically set by GitHub Actions / Vercel CI
# CI=true
